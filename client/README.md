# Client

React (Vite) frontend for the Customer Review & Rating Management System.

See the root [README.md](../README.md) for full project/architecture documentation.

## Scripts

- `npm run dev` – start the Vite dev server
- `npm run build` – production build
- `npm run preview` – preview the production build
- `npm run lint` – run oxlint
